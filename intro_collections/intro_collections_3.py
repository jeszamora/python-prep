# differences: list is mutable, tuple is immutable, lists use brackets
# tuples use parenthesis

# similarities: both are sequences, both are indexed from 0 to 
# len(collection - 1). Both are heterogeneous (elements do not 
# have to be the same type)

