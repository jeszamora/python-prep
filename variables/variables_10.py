print('Answered using comments.')

# obj = 'ABcd' reassignes the value of the variable to 'ABcd'
# obj.upper() does neither as it is an expression, not a statement
# obj = obj.lower() reassignes the values of the variable to another object 
# print(len(obj)) does neither is is an expression, not a statement
# obj = list(obj) reassigns the value of the variable to a new object
# obj.pop() mutates the values
# obj[x] = 'X' mutates the values
# obj.sort() mutates the values
# set(obj) does neither
# obj = tuple(obj) reassigns the value of the variable to a new object