# We use a for loop to iterate over the range object with the arguments 10, 0, and -1.
# 10 represnts our start point, 0 represents our stop point,
# non-inclusive, and -1 represents our step.
for i in range(10, 0, -1):
    print(i)

print('Launch!')